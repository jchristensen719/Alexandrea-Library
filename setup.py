from setuptools import setup, find_packages

setup(
    name="alexandrea-library",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        'setuptools',
    ],
    python_requires='>=3.8',
)
